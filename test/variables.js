const userName = "Abcd"
let userBatch = 1;
var userAge = 22;
userCity = "xyz";

/* we use constant in js because once we declare a value in const we can't re-declare in the same const. In simple words we use const to declare specified values. */

console.log(userName);

// we use console.table([]) to print more than one value together.

console.table([userBatch,userAge,userCity]);